Setup:

  Make sure to follow the guide here:
  https://developers.google.com/drive/quickstart-ruby#step_1_enable_the_drive_api

  At pt 5 I had to create a new Client ID and select 'other' as the Web-app did not work with the localhost redirection.

  After all that copy config/client_secret.yml{.example,} and change the id & secret
